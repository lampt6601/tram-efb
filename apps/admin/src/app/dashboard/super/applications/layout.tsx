import type { Metadata } from "next";

export const metadata: Metadata = { title: "Đơn Đăng Ký Bán Hàng" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
