import type { Metadata } from "next";

export const metadata: Metadata = { title: "Thêm Yêu Cầu Tìm Acc" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
