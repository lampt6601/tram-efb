import type { Metadata } from "next";

export const metadata: Metadata = { title: "Đăng Nhập | THC Admin" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
